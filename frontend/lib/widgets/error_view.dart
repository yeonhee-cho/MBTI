import 'package:flutter/material.dart';

class error_view extends StatelessWidget {
  const error_view({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('error_view is working'),
      ),
    );
  }
}