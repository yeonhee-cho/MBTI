import 'package:flutter/material.dart';

class GuestSection extends StatelessWidget {
  const GuestSection({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GuestSection is working'),
      ),
    );
  }
}